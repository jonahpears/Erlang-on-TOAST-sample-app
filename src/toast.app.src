{application, toast,
 [{description, "An OTP application"},
  {vsn, "0.1.0"},
  {registered, []},
  {mod, {toast_app, [
      % {role, #{module=>server_side,name=>server_side}},
      % {role, #{module=>client_side,name=>client_side}} 
      {role, #{module=>server_side_ext,name=>server_side}},
      {role, #{module=>client_side_ext,name=>client_side}} 
      % {role, #{module=>untimed_server,name=>untimed_server}},
      % {role, #{module=>untimed_client,name=>untimed_client}} 
    ]}},
  {applications,
   [kernel,
    stdlib
   ]},
  {env,[]},
  {modules, []},

  {licenses, ["Apache-2.0"]},
  {links, []}
 ]}.
